export enum UserTypes {
  admin = 'admin',
  employee = 'employee',
  client = 'client'
}

export enum RoleWeight {
  admin,
  employee,
  client,
}

export enum flightStatus {
  Scheduled = 'Scheduled',
  Completed = 'Completed',
}

